payload = Map();
payload.put("type", "scheduler");
payload.put("reason", "hourly_scan");

response = invokeurl
[
    url         : "https://your-domain.com/api/cliq/webhook"
    type        : POST
    contentType : "application/json"
    body        : payload.toString()
];

info response;
