payload = Map();
payload.put("source_user_id", param.get("user").get("id"));

response = invokeurl
[
    url         : "https://your-domain.com/api/cliq/command/mystress"
    type        : POST
    contentType : "application/json"
    body        : payload.toString()
];

resultText = if(response.get("text") != null, response.get("text"), "No data yet.");
return {"text": resultText};
